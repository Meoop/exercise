#include <iostream>
#include <vector>

using std::cin;
using std::cout;
using std::endl;
using std::vector;

int main()
{
    vector<int> icin;
    int v;
    while (cin >> v)
        icin.push_back(v);
    return 0;
}
